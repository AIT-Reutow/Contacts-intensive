spring:
  datasource:
    url: jdbc:mysql://localhost:3307/contacts
    username: root
    password: root

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate.SQL: DEBUG
